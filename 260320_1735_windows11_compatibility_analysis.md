# Windows 11 로컬 통합 테스트 인프라 환경 설정 분석 (260320_1735)

사용자가 승인한, Windows 11 OS 환경에서 발생하는 `redis-server`, `celery`, `uvicorn` 패키지(명령어) 인식 불가 오류에 대한 원인 분석 및 해결 방침입니다.

## 1. 외부 데몬 패키지 PATH 오류 추적 및 해결
- **문제점:** Windows CMD 혹은 PowerShell 터미널 환경에서 단순 파이썬 인터프리터 명령어가 아닌 `uvicorn`, `celery` 등 백그라운드 툴링 명령어를 직접 호출 시, 환경 변수 프로그램(Environment Variables: PATH) 꼬임 현상으로 실행 파일을 찾지 못하는 현상이 아주 빈번합니다.
- **아키텍처 스니펫 결단 (판단 근거):** 윈도우 터미널 전역 오류를 원천 차단하기 위해, 파이썬 인터프리터 자체에 내장 모듈 모드 옵션(`python -m 패키지명`)을 취해 감싸안아 실행하도록 매뉴얼 스니펫을 수정했습니다. 이 방식은 `python` 명령어가 구동되는 활성된 가상 환경(venv)이나 글로벌 사이트 경로 내에 설치된 동일한 버전의 모듈을 가장 확실하게 100% 잡아채어 호출하므로 안전합니다. 더욱이 윈도우에서의 Celery는 자식 프로세스 풀 생성 문제 데드락이 있으므로 `--pool=solo` 인자를 기본으로 붙이도록 강제할 것입니다.

## 2. Redis 브로커의 OS 네이티브 호환성 이슈 해결
- **문제점:** 백그라운드 기반 태스크 비동기 큐 구동을 위해 반드시 필요한 브로커(Broker)인 `redis-server`는 Linux, Unix 공식 지원 소프트웨어로서 Windows 네이티브 실행바이너리(.exe)를 제공하지 않습니다. 이 때문에 일반 윈도우 환경에서는 `redis-server is not recognized as an internal or external command` 오류가 무조건 터지게 되어 있습니다.
- **아키텍처 스니펫 결단 (판단 근거):** 이 AI 릴레이 시스템은 어댑터 클라이언트의 샌드박싱 구동(CLI 격리)을 위해 근본적으로 로컬 호스트에 **Docker 에이전트(Docker Desktop 등)**를 필수로 요구하고 있는 구조입니다. 그러므로 별도의 윈도우용 서드파티 포크 버전(Memurai 등)을 번거롭게 설치할 필요 없이, 가장 순정 형태의 **Redis 공식 Docker 이미지(`redis:latest`)**를 백그라운드 띄우기(`-d`) 모드로 포트 포워딩 연결(`-p 6379:6379`)하여 PC 메모리에 띄우는 것이 가장 확실하고 오동작이 없는(Resilient) 클라우드-네이티브 표준 아키텍처입니다.

## 3. 적용 결과
현재 작성 배포된 `operations_manual.md` 통합 시스템 운영 가이드 중 [1. 전체 시스템 통합 테스트(E2E) 방법] 파트를 윈도우(Windows) 및 크로스플랫폼 친화적으로 전면 개정/변경(코드 스니펫 교체) 반영했습니다.
