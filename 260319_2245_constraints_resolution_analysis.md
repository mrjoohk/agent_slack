# 제약사항 해결 방안 분석 및 확정 (260319_2245)

사용자의 피드백을 수용하여 초기 MVP 설계의 한계를 개선하는 방안을 분석 및 확정했습니다.

## 1. 슬랙 명령어 라우팅 (UX 개선)
- **피드백 요약:** 슬래시 커맨드(`/ai run --skill=...`) 대신 메신저처럼 자연스러운 `skill[skill_name] 요청내용` 방식을 사용하자.
- **결정 사항:** `app.message` 이벤트 리스너를 사용하여 사용자 채팅을 정규식(`^skill\[(.*?)\]\s*(.*)`)으로 파싱 처리.
- **판단 근거:** Slack 매니페스트(App Manifest)에 명시적인 슬래시 커맨드를 매번 추가해야 하는 운영 제약을 피할 수 있습니다. 동시에, 사용자가 스킬 이름을 대괄호`[]`를 통해 확정적으로 명시할 수 있으므로, 에이전트가 엉뚱한 스킬 컨텍스트를 로드하는 할루시네이션(임의 추측)을 완벽히 방지할 수 있는 가장 직관적인 해결책입니다.

## 2. 4,000자 초과 메세지 스트리밍 (로그 잘림 문제 해결)
- **피드백 요약:** 로그가 짤리지 않고 연속해서 계속 출력(append)되도록 개선하자.
- **결정 사항:** 버퍼가 4,000자에 도달하면 텍스트를 자르는(Truncation) 방식 대신, 기존 쓰레드에 **새로운 메시지 블록(Reply)**을 발행(`chat.postMessage`)하고, 그 새로운 메시지 블록(ts) 기준으로 다음 로그 청크를 갱신(`chat.update`)하도록 **페이지네이션(Pagination) 패턴**을 적용합니다.
- **판단 근거:** 슬랙 API의 Update 제한 한계(블록당 4000자)를 기술적으로 우회하면서도, 과거의 로그들이 절대로 유실되지 않고 사용자의 화면 스크롤을 통해 모두 남을 수 있는 무한 출력(Infinite stream) 경험을 보장하기 때문입니다.

## 3. 사전 제안의 추가 확정 사항
- **Docker 베이스 이미지:** `nikolaik/python-nodejs:latest` 사용 (단일 컨테이너로 파이썬 백엔드와 Anthropic npm CLI 동시 대응 가능)
- **마운트 보안 검증:** `.env`의 화이트리스트 변수 참조 방식
- **상태 저장소:** Redis Checkpointer 재활용 (기존 인프라 최대한 활용)
