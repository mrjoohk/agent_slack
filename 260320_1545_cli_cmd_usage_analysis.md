# CLI 자체 지원 슬래시 커맨드 활용 전략 분석 (260320_1545)

사용자가 제안한 "클라이언트(CLI) 도구 내부의 자체 지원 슬래시 명령어(예: `/cost`)를 활용하여 사용량 정보를 파싱한다"는 설계에 대한 분석 내용입니다.

## 1. 사용자의 신규 제안 요약 및 평가
- **제안 핵심:** 벤더사의 공식 API를 연동하거나, 비공식 로컬 설정 파일(`.json`)을 해킹하듯 스크래핑(Scraping)할 필요 없이 CLI에 내장된 토큰 확인용 슬래시 커맨드(예: `claude /cost`)를 어댑터 내부에서 단독 실행(one-off)하여 그 결과물을 낚아채자(Intercept).
- **설계 평가 (최상):** CLI 도구가 업데이트되더라도 공식 인터페이스(터미널 출력)는 가장 마지막에 변경/삭제될 확률이 높으므로, 서버 입장에서는 매우 견고하고 세련된(Resilient) 아키텍처 결단입니다.

## 2. 세부 구현 알고리즘 (동작 메커니즘 판단 근거)
슬랙 채널에서 봇이 사용자에게 **[CLI 및 모델 선택 모달 창]**을 그리기 직전 백그라운드에서 동작합니다.

1. **사전 트리거 (Hook):** 사용자가 슬랙에서 봇 호출 명령어(`@bot claude`)를 치면 Worker 큐에 비동기 태스크가 발동.
2. **어댑터 은닉 실행:** `ClaudeCLIAdapter` 클래스 내부의 새로운 인터페이스 `get_usage_limits()`가 단발성으로 백그라운드 실행되며, `claude --print "/cost"` 또는 `cursor --cmd "usage"` 형태의 보일러플레이트 서브프로세스를 기동합니다.
3. **응답 캡처 (Intercept):** CLI가 명령어의 결과로 표준 출력(stdout)에 내뱉는 원형 텍스트(예: "You have used 10,000 tokens (15%) remaining.")를 어댑터가 메모리로 넘겨받습니다.
4. **추출 및 포맷팅:** Regex(정규 표현식)나 경량화된 LLM 시스템 프롬프트를 통해 원시 텍스트 문장에서 퍼센티지(%) 혹은 비용($) 숫자만 딱 뽑아냅니다.
5. **Slack 화면 렌더링:** 최종적으로 슬랙 채널에 반환될 드롭다운 선택지에 동적 주입(`예: [선택] Claude 3.5 Sonnet (남은 한도: 15%)]`) 후 모달 창을 그려냅니다.

## 3. 적용 계획
해당 최고 효율점 아키텍처 픽업이 승인되었으므로, 작성된 기존의 `operations_manual.md` 내용 중 '사용량(Quota)' 파싱 문단을 이 스니펫 전용 캡처 전략으로 수정 반영하고 에이전트 설계 사상을 최종 완결합니다.
