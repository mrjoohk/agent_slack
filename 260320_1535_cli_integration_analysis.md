# 다중 CLI 연동 및 사용량(Quota) 조회 전략 분석 (260320_1535)

Claude 뿐만 아니라 Codex, Gemini, Cursor를 모두 지원하며, 슬랙 UI에 "사용 가능 모델"과 "사용 가능량(%)"을 표시하기 위한 아키텍처 검토입니다.

## 1. CLI의 근본적 한계와 이슈 정의
AI CLI 도구(`claude-code`, `cursor`, `gemini-cli` 등)는 프로그램 간 통신(Machine-to-Machine)이 아닌 '사람'이 터미널에서 읽고 쓰도록 만들어져 있습니다.
따라서 **"현재 내 계정의 모델별 남은 잔여량(%)을 JSON으로 반환해 줘"** 같은 공통 표준 API 명령어가 존재하지 않습니다. 잔여량이 소진되었을 때 텍스트 경고 메시지만 출력할 뿐입니다.

## 2. 사용 가능 모델 및 잔여량 수집 방안 분석 (판단 근거)

### A. 사용 가능 모델 목록 동적 구성 (구현 용이)
- **방안:** 시스템의 `config.yaml`이나 DB에 각 CLI별 지원 모델 하드코딩 매핑(예: `claude: [Haiku 4.5, Sonnet 4.6, Opus 4.6]`, `cursor: [Claude-3.5-Sonnet, GPT-4o]`)을 구성합니다.
- **동작:** 슬랙에서 `@bot claude`를 호출하면, 봇은 이 설정 파일을 읽고 즉각 팝업 모달(드롭다운 박스)의 선택지로 모델 리스트를 렌더링합니다.

### B. 잔여량(%) 추출 방안 3가지
**[옵션 1] 공식 Billing API 백도어 호출 (가장 정확, MVP 외부)**
- CLI 도구를 거치지 않고, CLI가 사용하는 API Key(Anthropic/OpenAI)를 서버가 직접 쥐고 각 벤더의 공식 Usage API 엔드포인트를 호출해 남은 토큰 크레딧 한도를 계산합니다. (다만 유료 구독형인 Cursor의 경우 별도 API가 없을 수 있어 한계가 있음)

**[옵션 2] CLI 내부 로컬 캐시 스크래핑 파싱 (유지보수 취약)**
- Cursor나 Claude CLI가 사용량 한도에 도달할 때 내부 폴더(예: `~/.cursorapp/usage.db` 또는 `~/.claude/config.json`)에 남기는 메타데이터를 정규식으로 긁어모아 %로 환산합니다. (CLI가 업데이트되면 로직이 깨질 위험 큼)

**[옵션 3] 사후 에러 캡처 방식 (Graceful Degradation - MVP 추천)**
- 슬랙 UI의 모델 선택 창에서는 "사용 가능량(%)"을 노출하지 않거나, 과거의 캐시 된 상태만 보여줍니다.
- 대신 사용자가 요청 전송 후, CLI가 내부적으로 토큰/사용량 소진 메시지(`RateLimitError`, `QuotaExceeded` 등)를 터미널(stdout)로 뿜어내면 `BaseCLIAdapter`가 이 문자열 패턴을 감지(Intercept)하여, 슬랙 대화방에 *"❌ Sonnet 4.6 사용량이 모두 소진되었습니다. Opus 4.6로 다시 시도해 주세요."* 라고 안내 팝업을 띄우는 방식입니다.

## 3. 적용 계획
- 본 문서를 참조하여 이후 배포될 `operations_manual.md`에 다중 CLI 추가 및 Adapter 상속 구조, 그리고 에러 캡처를 통한 간접적인 잔여량 알림 체계를 명시하겠습니다.
