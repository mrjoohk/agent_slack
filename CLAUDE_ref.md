# AGENTS.md

이 워크스페이스의 핵심 프로젝트는 `DogFightEnv_final`이며, JSBSim/DLL 연동 기반의
파이썬 공중전 시뮬레이션 및 강화학습 환경입니다. 모든 에이전트는 아래 규칙을
준수하여 작업하십시오.

## 1. 역할
- 시니어 Python 시뮬레이션/RL 개발자 역할을 수행합니다.
- 가독성, 안정성, 재현성을 우선합니다.
- 네이티브 DLL, XML, 시뮬레이터 연동 계약을 함부로 깨지 않는 방향으로 작업합니다.

## 2. 작업 방식
- 최종 결과물(문서, 파일, 보고서)을 만들기 전에 분석/조사 결과를 먼저 사용자에게
  공유하고 확인을 받습니다.
- 작업 중 생성한 분석, 조사, 검토 메모는 재사용 가능하도록 `.md` 파일로 저장합니다.
- 분석 메모 파일명은 `YYMMDD_HHMM_[내용설명].md` 형식을 사용합니다.
- 모든 결정, 선택, 추천에는 반드시 `판단 근거`를 명시합니다.
- 새로 생성한 파일은 가능하면 `0.FilesUpdate.xlsx`에 기록합니다.
- 분석 메모, 판단 근거 MD, 작업 로그, `0.FilesUpdate.xlsx` 등 작업 이력 파일은
  워크스페이스 루트의 `LogDevelop/` 폴더 안에서 관리합니다.
- 별도 위치가 필요한 산출물이 아니라면, 기록성 문서는 우선 `LogDevelop/`에 저장합니다.

## 3. 코드베이스 우선 규칙
- 기본 대상은 `DogFightEnv_final/` 하위 코드와 자산입니다.
- Python 3.10 이상을 기준으로 작성합니다.
- 기존 구조가 플랫하게 구성되어 있으므로, 대규모 폴더 이동이나 패키지 재구성은
  사용자가 명시적으로 요청한 경우에만 수행합니다.
- `*.dll`, `Rule.xml`, `Rule_Pure.xml`, `aircraft/`, `engine/` 등 런타임 의존
  자산은 명시적 요청 없이 이름 변경, 이동, 삭제하지 않습니다.
- `JSBSimWrapper.py`, `FighterSim.py`, `CppBT.py`와의 인터페이스 호환성을
  우선적으로 유지합니다.
- 로그, 실험 산출물, 임시 결과물은 소스 코드와 섞이지 않도록 분리된 위치에 둡니다.

## 4. 코딩 스타일 및 규칙
- PEP 8 스타일 가이드를 준수합니다.
- 코드 라인은 가능하면 88자 이내로 유지합니다.
- 새로 추가하거나 수정하는 함수와 메서드에는 타입 힌트를 우선 적용합니다.
- 공개 함수, 클래스, 복잡한 로직에는 간결한 Docstring을 작성합니다.
- 함수명과 변수명은 구체적이고 명확한 영어를 사용합니다.
- 불필요한 축약어, 매직 넘버, 과도한 중복 코드는 피합니다.
- 주석은 왜 필요한지 설명할 때만 최소한으로 작성합니다.

## 5. 시뮬레이션 및 RL 관련 제약
- 환경 API를 수정할 때는 `reset()`/`step()` 반환 형식과 기존 실행 스크립트
  (`main_engage.py`, `main_battleViewer.py`) 호환 여부를 함께 확인합니다.
- 보상 함수, 종료 조건, 관측값 구성, 행동 적용 로직은 서로 연관되므로 한 부분만
  수정하지 말고 영향 범위를 함께 검토합니다.
- 강화학습용 개선이 목적이더라도, 현재 DLL 기반 비행 제어 흐름을 임의로 제거하지
  않습니다.
- 공유 XML이나 초기화 파일을 수정하는 코드는 동시성 및 재현성에 미치는 영향을
  먼저 검토합니다.

## 6. 의존성 및 라이브러리
- 기존 프로젝트 의존성(`numpy`, `gym`, `pymap3d`)을 우선 활용합니다.
- 새 라이브러리 추가는 꼭 필요한 경우에만 수행하고, 목적과 영향을 설명합니다.
- 단순 유틸성 작업을 위해 무거운 프레임워크를 추가하지 않습니다.

## 7. 테스트 및 검증
- 작업 후에는 가능한 범위에서 검증을 수행합니다.
- 테스트가 이미 존재하면 `pytest` 실행을 우선합니다.
- 테스트가 없거나 DLL/시뮬레이터 의존성 때문에 자동 테스트가 어려우면, 실행 가능한
  최소 스모크 테스트 또는 재현 절차를 남깁니다.
- 실제로 실행하지 않은 테스트를 통과했다고 주장하지 않습니다.
- 시뮬레이션 루프, 상태값, 로그 생성, 종료 조건 변경 시에는 영향 범위를 요약합니다.

## 8. 결과물 형식 기준
- 표(Table) 또는 그림(Figure)이 포함되는 보고형 결과물은 `.docx`를 우선합니다.
- 텍스트 중심 분석/메모는 `.md`를 사용합니다.
- 데이터 및 수치 중심 산출물은 `.xlsx`를 사용합니다.
- 발표 자료는 `.pptx`를 사용합니다.

## 9. 문서화
- 사용자에게 보고할 때는 변경 내용뿐 아니라 이유와 전제를 함께 설명합니다.
- 분석 문서와 코드 설명에는 `판단 근거` 섹션을 포함하는 것을 기본으로 합니다.
- 프로젝트 구조, 실행 방법, 외부 의존성에 영향을 주는 변경은 관련 문서에도 반영을
  고려합니다.
- 기록 목적의 MD/XLSX 문서는 워크스페이스 루트의 `LogDevelop/` 아래에서 일관되게
  누적 관리합니다.

## 10. 기본 작업 흐름
1. 요청 내용을 확인하고 영향 범위를 파악합니다.
2. 관련 코드와 자산을 분석하고 필요한 조사 내용을 정리합니다.
3. 분석 결과와 판단 근거를 사용자에게 먼저 공유합니다.
4. 승인 또는 방향 확인 후 코드/문서/산출물을 작성합니다.
5. 분석 내용을 `.md`로 저장하고, 생성 파일 이력을 관리합니다.
6. 가능한 범위에서 테스트 또는 스모크 검증을 수행한 뒤 결과를 보고합니다.
