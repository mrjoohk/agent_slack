# 슬랙 대화형(Wizard) 모델 선택 플로우 분석 (260320_1525)

사용자가 제안한 "단계적 CLI/모델 선택 및 프롬프트 입력" 순차 플로우에 대한 아키텍처 및 UX 분석 내용입니다.

## 1. 사용자의 제안 아키텍처 요약 및 평가
- **제안 핵심:** 슬랙 봇은 복잡한 자율 판단(Multi-agent Orchestration)을 스스로 하지 않고, 단순히 사용자가 어떤 CLI의 어떤 모델을 쓸 것인지만 순차적으로 인터뷰(`1. 봇 멘션 -> 2. 모델 응답 -> 3. 모델 선택 -> 4. 프롬프트 요구 -> 5. 프롬프트 입력`)한 뒤, 타겟 CLI에 터미널 명령으로 넘겨주는 순수 **릴레이 봇(Proxy Bot)** 역할을 수행한다.
- **아키텍처 평가 (훌륭함):** 이렇게 봇의 역할을 최소화하고 CLI 자체의 기능(`claude-code`, `cursor`)에 LLM 실행맥락을 완전히 위임하면, 상태 관리 로직이 극도로 단순해지며 봇 서버의 시스템 오류(SPOF) 지점이 사라집니다. 백엔드 시스템 유지보수 관점에서 훨씬 안전하고 확실한(Deterministic) 매우 좋은 설계 방향입니다.

## 2. 순차적 채팅(Text-pingpong) 방식의 한계와 해결책 제안 (판단 근거)
제안하신 1~7단계 텍스트 핑퐁 플로우를 그대로 채팅창으로만 구현하려면, 백엔드에서 사용자별/세션별 **상태 머신(State Machine)**을 메모리나 Redis에 저장하여 다음 턴의 채팅이 "모델 입력 단계"인지 "프롬프트 입력 단계"인지 지속 추적해야 합니다.
- **순수 텍스트 입력의 부작용:** 사용자가 "Sonnet 4.6" 대신 오타로 "sonet"을 치거나 대화 흐름을 중단했을 경우의 예외 처리 로직이 복잡해지는 단점이 있습니다.
- **대안: [Slack Block Kit (드롭다운/모달) 활용]**
  1. 슬랙에는 텍스트를 복잡하게 파싱할 필요가 없는 직관적인 **인터랙티브 UI(Block Kit)**가 있습니다.
  2. 사용자가 `@bot claude`라고 호출하면, 봇이 텍스트로 되묻는 대신 **[사용 가능 모델 선택 드롭다운 리스트]**와 **[프롬프트 텍스트 멀티라인 입력란]**이 포함된 팝업 모달창(또는 버튼UI)을 즉시 띄워줍니다.
  3. 사용자는 창 상에서 마우스로 모델을 편하게 고르고 지시를 입력해 `[전송]` 버튼 한 번만 누르면 됩니다.
  4. 서버는 오타가 전혀 없는 완벽한 데이터 페이로드(`model=sonnet-4.6`, `prompt=...`)를 단 한 번의 이벤트로 수신하고 CLI 파라미터로 넘겨줄 수 있습니다.

*결론적으로, 봇의 릴레이/프록시 구조 사상(제안해주신 봇의 역할 축소)은 적극 채용하되, 텍스트 형태의 핑퐁보다는 Slack이 기본 제공하는 네이티브 모달(Modal) UI를 쓰면 개발 복잡도는 낮아지고 사용자 사용성(UX)은 대폭 상승합니다.*

## 3. 적용 계획
제안해주신 봇의 역할(단순 CLI 릴레이 프록시) 정의를 설계 사상에 반영하고, 위에서 제안드린 Slack Modal 형태 또는 세션 상태 기반 핑퐁 중 선택된 인터페이스의 실제 작동 절차도를 매뉴얼 및 구현 계획에 통합하겠습니다.
