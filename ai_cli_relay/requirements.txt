# ai_cli_relay dependencies

# Core
fastapi==0.110.0      # 현재는 미사용 (Socket Mode로 전환). 향후 REST API 확장 시 재활성화
uvicorn==0.28.0       # 위와 동일
pydantic==2.6.4
python-dotenv>=1.0.0  # .env 자동 로드 (bat 파일 없이 직접 실행해도 동작)

# Slack Integration — Socket Mode (포트포워딩/ngrok 불필요)
slack_bolt==1.18.1
websockets==12.0      # AsyncSocketModeHandler 의존성

# Task Queue (현재 미사용 — asyncio.create_task() 기반으로 대체됨)
# Celery + Redis는 멀티 서버 분산 처리가 필요한 경우에만 활성화
# celery==5.3.6
# redis==5.0.3

# Docker SDK for Sandbox Execution
docker==7.0.0

# Orchestration Engine
langchain==0.1.13
langgraph==0.0.30

# Excel utility
openpyxl==3.1.2
