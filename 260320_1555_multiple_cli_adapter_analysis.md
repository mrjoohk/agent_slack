# 다중 CLI 연동 및 쉘 인젝션 방어 설계 (260320_1555)

사용자가 승인한, 릴레이용 "서버 PC 설정 및 다중 CLI 어댑터 구현 전략"에 대한 핵심 분석 및 아키텍처 결단 내용입니다.

## 1. 서버/데스크탑 릴레이 PC의 가장 중요한 '격리' 보안
- **판단 근거 (위험 요소 회피):** 사용자가 슬랙 모달의 `프롬프트` 창에 악의적이거나 잘못된 OS 실행 구문(예: `; rm -rf /` 등)을 입력했을 때, 서버 PC가 이를 방어해야 합니다.
- **아키텍처 설계:** `Command Injection` 취약점을 원천 봉쇄하기 위하여 파이썬 모듈이 `shell=True`를 절대 허용하지 않습니다. 모든 어댑터(`Cursor`, `Gemini` 등) 모듈은 `create_subprocess_exec` 함수를 통해 사용자의 프롬프트를 반드시 쪼개진 '안전한 개별 인자(String Argument Array)'로만 넘깁니다.
  - *예시: ❌ `claude "내용; format c:"` (쉘 파악) -> ✅ `["claude", "내용; format c:"]` (쉘을 타지 않고 단순 긴 텍스트로만 샌드박스 내부 취급)*

## 2. 팩토리 (Factory Pattern) 라우터 추가 확립
- **판단 지표:** `Cursor(agent)`, `Gemini`, `Codex`, `Claude` 4가지의 CLI 도구가 각각 다른 파라미터와 호출명을 가집니다.
- 워커 최하단인 `tasks.py` 모듈에서 사용자가 Slack 모달/버튼 창에서 최종적으로 셀렉트한 `selected_cli=cursor` 페이로드 데이터 값을 취합합니다.
- 이에 맞게 `if-elif` 팩토리 분기문을 타서 오직 지정된 특정 어댑터 구상체(Instance) 하나만 기동한 뒤 커맨드를 주입하므로, 에이전트 확장성에 있어서 OCP(Open-Closed Principle: 개방 폐쇄 원칙)를 완벽히 준수하게 됩니다.

이후 작성될 모든 3종 파이썬 모듈 및 `tasks.py` 수정에 해당 사항들을 즉각 투영합니다.
